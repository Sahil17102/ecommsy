name: box-and-beyond-services

services:
  backend:
    image: box-and-beyond-services-backend
    container_name: box-and-beyond-services-backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:3001"
    env_file:
      - .env
    restart: always

    networks:
      - default
      - internal

networks:
  internal:
    external: true